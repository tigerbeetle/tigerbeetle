# By default, `dotnet format` uses `\r\n` line endings on Windows. To override this behavior, we
# need this editorconfig file.
root = true

[*]
end_of_line = lf
insert_final_newline = true
charset=utf-8
indent_style=space
indent_size=4
